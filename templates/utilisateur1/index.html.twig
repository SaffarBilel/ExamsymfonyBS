{% extends 'base.html.twig' %}

{% block title %}Utilisateur index{% endblock %}

{% block body %}
    <h1>Utilisateur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Adresseemail</th>
                <th>Role</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for utilisateur in utilisateurs %}
            <tr>
                <td>{{ utilisateur.id }}</td>
                <td>{{ utilisateur.Nom }}</td>
                <td>{{ utilisateur.Adresseemail }}</td>
                <td>{{ utilisateur.Role }}</td>
                <td>
                    <a href="{{ path('app_utilisateur1_show', {'id': utilisateur.id}) }}">show</a>
                    <a href="{{ path('app_utilisateur1_edit', {'id': utilisateur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_utilisateur1_new') }}">Create new</a>
{% endblock %}
