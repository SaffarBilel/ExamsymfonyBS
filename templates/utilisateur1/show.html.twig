{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
    <h1>Utilisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ utilisateur.Nom }}</td>
            </tr>
            <tr>
                <th>Adresseemail</th>
                <td>{{ utilisateur.Adresseemail }}</td>
            </tr>
            <tr>
                <th>Role</th>
                <td>{{ utilisateur.Role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_utilisateur1_index') }}">back to list</a>

    <a href="{{ path('app_utilisateur1_edit', {'id': utilisateur.id}) }}">edit</a>

    {{ include('utilisateur1/_delete_form.html.twig') }}
{% endblock %}
